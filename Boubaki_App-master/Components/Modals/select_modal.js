import React,{  useEffect, useRef, useState } from "react";
import {View, Button,Pressable,FlatList,Image,Text,Modal, StyleSheet, TouchableNativeFeedback, ToastAndroid, SafeAreaView, ActivityIndicator,} from "react-native"

const SelectModal=(props) => {
    return <View>

    </View>
}

const styles= StyleSheet.create({
    modalView: {
        backgroundColor: "white",
        width:'80%',
        height:'30%',
        borderRadius: 20,
        paddingTop: 10,
        justifyContent:'space-between',
        alignItems: "center",
        shadowColor: "#000",
        shadowOffset: {
          width: 0,
          height: 2
        },
        shadowOpacity: 0.25,
        shadowRadius: 3.84,
        elevation: 5
      }
})